{
  "name": "leet-questions",
  "version": "1.1.0",
  "type": "module",
  "private": true,
  "description": "Stop losing your progress and start building your developer portfolio with automatic accepted LeetCode solution sync to GitHub.",
  "scripts": {
    "setup": "npm i",
    "format": "prettier --write **/*.{js,css,html,json,md}",
    "format-test": "prettier --check **/*.{js,css,html,json,md}",
    "lint": "eslint . --fix",
    "lint-test": "eslint . --color"
  },
  "devDependencies": {
    "@eslint/js": "^9.36.0",
    "eslint": "^9.36.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-prettier": "^5.5.4",
    "globals": "^16.4.0",
    "prettier": "^3.6.2"
  }
}
